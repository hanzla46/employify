{
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://employify-backend.vercel.app/:path*" }
  ],
  "redirects": [
    { "source": "/:path", "destination": "/:path", "statusCode": 200 }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
